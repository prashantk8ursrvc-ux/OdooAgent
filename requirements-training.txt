# Fine-tuning stage. Install into its own environment - unsloth pins torch
# closely and will fight anything else sharing the venv.
#
# Versions are the ones this model was actually trained with on Windows +
# CUDA 12.8. torch must match your CUDA; see https://pytorch.org for the
# correct index URL, e.g.
#     pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.10.0
unsloth==2026.6.9
transformers==5.5.0
trl==0.24.0
peft==0.19.1
bitsandbytes==0.49.2
accelerate==1.14.0
datasets==4.3.0
